#!/usr/bin/perl

use strict;
use warnings;

print "I'm going to docker exec into this container to play.
 For it to exist and be on the docker-compose network
 it needs to do something - else it'll simply exit.\n";

sleep 86400;

